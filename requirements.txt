google-genai>=2.10.0,<3
openai>=2.45.0,<3
pydantic>=2.0,<3
PyAudioWPatch>=0.2.12
numpy>=1.26
scipy>=1.11
python-dotenv>=1.0
websockets>=12
opencc-python-reimplemented>=0.1.7

# Optional — only for transcript speaker labels (設定→標記講者).
# Pulls PyTorch (~1-2 GB). Skip if you don't use the feature:
#   pip install resemblyzer
